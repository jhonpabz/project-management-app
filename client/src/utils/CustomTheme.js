import { createTheme } from "@mui/material";

export const CustomTheme = createTheme({
  palette: {
    primary: {
      main: "#111827",
    },
    secondary: {
      main: "#02b0fe",
    },
  },
});
